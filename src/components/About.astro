---
---

<section id="about" class="about">
	<div class="container">
		<h2>Sobre Mí</h2>
		<div class="about-content">
			<div class="about-image">
				<img src="https://images.pexels.com/photos/3861458/pexels-photo-3861458.jpeg?auto=compress&cs=tinysrgb&w=600&h=400&fit=crop" alt="Workspace moderno" class="workspace-image">
				<div class="image-overlay">
					<div class="tech-icons">
						<span class="tech-icon">⚛️</span>
						<span class="tech-icon">🚀</span>
						<span class="tech-icon">💻</span>
						<span class="tech-icon">🎯</span>
					</div>
				</div>
			</div>
			<div class="about-text">
				<div class="text-highlight">
					<span class="highlight-number">01</span>
					<h3>Pasión por la Tecnología</h3>
				</div>
				<p>
					Soy un desarrollador full-stack apasionado por crear soluciones web innovadoras 
					y de alto rendimiento. Con experiencia en tecnologías modernas, me dedico a resolver 
					problemas complejos y ayudar a los equipos a alcanzar sus objetivos.
				</p>
				<div class="text-highlight">
					<span class="highlight-number">02</span>
					<h3>Experiencia Técnica</h3>
				</div>
				<p>
					Me especializo en el desarrollo tanto del frontend como del backend, 
					trabajando con tecnologías como React, Node.js, TypeScript y bases de datos relacionales 
					y no relacionales.
				</p>
				<div class="text-highlight">
					<span class="highlight-number">03</span>
					<h3>Compromiso Continuo</h3>
				</div>
				<p>
					Fuera del código, disfruto aprendiendo nuevas tecnologías, contribuyendo a proyectos 
					de código abierto y mentorando a otros desarrolladores.
				</p>
				<div class="skills-preview">
					<div class="skill-item">
						<span class="skill-icon">🤖</span>
						<span>AI Development</span>
					</div>
					<div class="skill-item">
						<span class="skill-icon">⚛️</span>
						<span>React/Vue</span>
					</div>
					<div class="skill-item">
						<span class="skill-icon">🚀</span>
						<span>Node.js</span>
					</div>
					<div class="skill-item">
						<span class="skill-icon">🐳</span>
						<span>Docker</span>
					</div>
				</div>
			</div>
		</div>
	</div>
</section>

<style>
	.about {
		padding: 5rem 2rem;
		background-color: var(--color-primary);
		position: relative;
	}

	.about::before {
		content: '';
		position: absolute;
		top: 0;
		left: 0;
		right: 0;
		height: 1px;
		background: linear-gradient(90deg, transparent, var(--color-accent), transparent);
	}

	.container {
		max-width: 1200px;
		margin: 0 auto;
	}

	.about h2 {
		text-align: center;
		margin-bottom: 3rem;
		background: linear-gradient(135deg, var(--color-gradient-1), var(--color-gradient-2));
		-webkit-background-clip: text;
		-webkit-text-fill-color: transparent;
		background-clip: text;
		position: relative;
	}

	.about h2::after {
		content: '';
		position: absolute;
		bottom: -10px;
		left: 50%;
		transform: translateX(-50%);
		width: 60px;
		height: 3px;
		background: linear-gradient(90deg, var(--color-accent), var(--color-accent-light));
		border-radius: 2px;
	}

	.about-content {
		display: grid;
		grid-template-columns: 1fr 1fr;
		gap: 3rem;
		align-items: center;
	}

	.about-image {
		position: relative;
		border-radius: 1rem;
		overflow: hidden;
		box-shadow: var(--shadow-medium);
		transition: transform 0.3s ease;
	}

	.about-image:hover {
		transform: translateY(-10px);
	}

	.workspace-image {
		width: 100%;
		height: 400px;
		object-fit: cover;
		transition: transform 0.3s ease;
	}

	.about-image:hover .workspace-image {
		transform: scale(1.05);
	}

	.image-overlay {
		position: absolute;
		top: 0;
		left: 0;
		right: 0;
		bottom: 0;
		background: linear-gradient(135deg, rgba(59, 130, 246, 0.2), rgba(147, 51, 234, 0.2));
		display: flex;
		align-items: center;
		justify-content: center;
		opacity: 0;
		transition: opacity 0.3s ease;
	}

	.about-image:hover .image-overlay {
		opacity: 1;
	}

	.tech-icons {
		display: flex;
		gap: 1rem;
	}

	.tech-icon {
		font-size: 2rem;
		animation: float 3s ease-in-out infinite;
	}

	.tech-icon:nth-child(2) { animation-delay: 0.5s; }
	.tech-icon:nth-child(3) { animation-delay: 1s; }
	.tech-icon:nth-child(4) { animation-delay: 1.5s; }

	.text-highlight {
		display: flex;
		align-items: center;
		gap: 1rem;
		margin: 2rem 0 1rem 0;
	}

	.highlight-number {
		background: linear-gradient(135deg, var(--color-accent), var(--color-accent-light));
		color: white;
		width: 40px;
		height: 40px;
		border-radius: 50%;
		display: flex;
		align-items: center;
		justify-content: center;
		font-weight: bold;
		font-size: 1.1rem;
		flex-shrink: 0;
	}

	.text-highlight h3 {
		color: var(--color-accent-light);
		margin: 0;
		font-size: 1.2rem;
	}

	.about-text p {
		font-size: 1.05rem;
		color: var(--color-text-muted);
		margin-bottom: 1.5rem;
		line-height: 1.8;
	}

	.skills-preview {
		display: grid;
		grid-template-columns: repeat(2, 1fr);
		gap: 1rem;
		margin-top: 2rem;
	}

	.skill-item {
		display: flex;
		align-items: center;
		gap: 0.75rem;
		padding: 0.75rem;
		background: var(--color-secondary);
		border-radius: 0.5rem;
		border: 1px solid var(--color-border);
		transition: all 0.3s ease;
	}

	.skill-item:hover {
		border-color: var(--color-accent);
		transform: translateX(5px);
		box-shadow: var(--shadow-soft);
	}

	.skill-icon {
		font-size: 1.5rem;
	}

	@media (max-width: 768px) {
		.about {
			padding: 3rem 1rem;
		}

		.about-content {
			grid-template-columns: 1fr;
		}

		.skills-preview {
			grid-template-columns: 1fr;
		}

		.about-text p {
			font-size: 1rem;
		}
	}
</style>
