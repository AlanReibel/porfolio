---
const skills = {
	aiDevelopment: ['Claude Code', 'Windsurf', 'Agents', 'MCP Integrations', 'Workflows'],
	frontend: ['HTML5', 'CSS3', 'JavaScript (ES6+)', 'TypeScript', 'Vue.js', 'Nuxt.js', 'Astro', 'React', 'Next.js', 'Three.js', 'Web Components'],
	backend: ['PHP', 'Laravel', 'SQL', 'Node.js', 'REST', 'SOAP'],
	testingQuality: ['Vitest', 'PHPUnit', 'Code Reviews', 'Clean Architecture'],
	devopsTools: ['Docker', 'Git (GitHub, GitLab)', 'Linux Bash', 'Apache', 'nginx', 'CI/CD'],
	performance: ['Web Performance Optimization', 'Core Web Vitals', 'Technical SEO', 'On-Page SEO'],
	designTools: ['Figma', 'Photoshop', 'Adobe XD', 'Sketch'],
	cmsEcommerce: ['WordPress', 'Prestashop'],
	learning: ['PostgreSQL', 'Kafka', 'AWS S3', 'TDD', 'DDD', 'FFB']
};
---

<section id="skills" class="skills">
	<div class="container">
		<h2>Habilidades</h2>
		<p class="section-subtitle">Tecnologías y herramientas que domino</p>
		<div class="skills-grid">
			<div class="skill-category">
				<div class="category-icon">🤖</div>
				<h3>AI & Development</h3>
				<div class="skill-tags">
					{skills.aiDevelopment.map(skill => (
						<span class="skill-tag">{skill}</span>
					))}
				</div>
			</div>
			<div class="skill-category">
				<div class="category-icon">🎨</div>
				<h3>Frontend</h3>
				<div class="skill-tags">
					{skills.frontend.map(skill => (
						<span class="skill-tag">{skill}</span>
					))}
				</div>
			</div>
			<div class="skill-category">
				<div class="category-icon">⚙️</div>
				<h3>Backend</h3>
				<div class="skill-tags">
					{skills.backend.map(skill => (
						<span class="skill-tag">{skill}</span>
					))}
				</div>
			</div>
			<div class="skill-category">
				<div class="category-icon">🧪</div>
				<h3>Testing & Quality</h3>
				<div class="skill-tags">
					{skills.testingQuality.map(skill => (
						<span class="skill-tag">{skill}</span>
					))}
				</div>
			</div>
			<div class="skill-category">
				<div class="category-icon">🚀</div>
				<h3>DevOps & Tools</h3>
				<div class="skill-tags">
					{skills.devopsTools.map(skill => (
						<span class="skill-tag">{skill}</span>
					))}
				</div>
			</div>
			<div class="skill-category">
				<div class="category-icon">⚡</div>
				<h3>Performance & SEO</h3>
				<div class="skill-tags">
					{skills.performance.map(skill => (
						<span class="skill-tag">{skill}</span>
					))}
				</div>
			</div>
			<div class="skill-category">
				<div class="category-icon">🎭</div>
				<h3>Design Tools</h3>
				<div class="skill-tags">
					{skills.designTools.map(skill => (
						<span class="skill-tag">{skill}</span>
					))}
				</div>
			</div>
			<div class="skill-category">
				<div class="category-icon">🛒</div>
				<h3>CMS & eCommerce</h3>
				<div class="skill-tags">
					{skills.cmsEcommerce.map(skill => (
						<span class="skill-tag">{skill}</span>
					))}
				</div>
			</div>
			<div class="skill-category learning-category">
				<div class="category-icon">📚</div>
				<h3>Actualmente Aprendiendo</h3>
				<div class="skill-tags">
					{skills.learning.map(skill => (
						<span class="skill-tag learning-tag">{skill}</span>
					))}
				</div>
			</div>
		</div>
	</div>
</section>

<style>
	.skills {
		padding: 5rem 2rem;
		background: linear-gradient(135deg, var(--color-secondary) 0%, #1a202c 100%);
		position: relative;
	}

	.skills::before {
		content: '';
		position: absolute;
		top: 0;
		left: 0;
		right: 0;
		height: 1px;
		background: linear-gradient(90deg, transparent, var(--color-accent), transparent);
	}

	.container {
		max-width: 1200px;
		margin: 0 auto;
	}

	.skills h2 {
		text-align: center;
		margin-bottom: 3rem;
		background: linear-gradient(135deg, var(--color-gradient-3), var(--color-gradient-4));
		-webkit-background-clip: text;
		-webkit-text-fill-color: transparent;
		background-clip: text;
		position: relative;
	}

	.skills h2::after {
		content: '';
		position: absolute;
		bottom: -10px;
		left: 50%;
		transform: translateX(-50%);
		width: 60px;
		height: 3px;
		background: linear-gradient(90deg, var(--color-gradient-3), var(--color-gradient-4));
		border-radius: 2px;
	}

	.section-subtitle {
		text-align: center;
		color: var(--color-text-muted);
		font-size: 1.1rem;
		margin-bottom: 3rem;
		font-weight: 300;
	}

	.skills-grid {
		display: grid;
		grid-template-columns: repeat(auto-fit, minmax(300px, 1fr));
		gap: 2rem;
	}

	.skill-category {
		background: rgba(255, 255, 255, 0.02);
		backdrop-filter: blur(10px);
		border: 1px solid rgba(255, 255, 255, 0.1);
		border-radius: 1rem;
		padding: 2rem;
		transition: all 0.3s cubic-bezier(0.4, 0, 0.2, 1);
		position: relative;
		overflow: hidden;
	}

	.skill-category::before {
		content: '';
		position: absolute;
		top: 0;
		left: 0;
		right: 0;
		height: 3px;
		background: linear-gradient(90deg, var(--color-accent), var(--color-accent-light));
		transform: scaleX(0);
		transition: transform 0.3s ease;
	}

	.skill-category:hover::before {
		transform: scaleX(1);
	}

	.skill-category:hover {
		transform: translateY(-10px);
		box-shadow: var(--shadow-medium);
		border-color: rgba(59, 130, 246, 0.3);
	}

	.category-icon {
		font-size: 2.5rem;
		margin-bottom: 1rem;
		display: block;
		animation: float 3s ease-in-out infinite;
	}

	.skill-category h3 {
		color: var(--color-accent-light);
		margin-bottom: 1.5rem;
		font-size: 1.3rem;
	}

	.skill-category.learning-category h3 {
		color: #fbbf24;
	}

	.learning-category .category-icon {
		filter: sepia(1) hue-rotate(30deg) saturate(2);
	}

	.skill-tags {
		display: flex;
		flex-wrap: wrap;
		gap: 0.75rem;
	}

	.skill-tag {
		background: linear-gradient(135deg, rgba(59, 130, 246, 0.1), rgba(59, 130, 246, 0.05));
		color: var(--color-accent);
		padding: 0.75rem 1.25rem;
		border-radius: 2rem;
		border: 1px solid var(--color-accent);
		font-size: 0.9rem;
		transition: all 0.3s cubic-bezier(0.4, 0, 0.2, 1);
		position: relative;
		overflow: hidden;
	}

	.skill-tag::before {
		content: '';
		position: absolute;
		top: 0;
		left: -100%;
		width: 100%;
		height: 100%;
		background: linear-gradient(90deg, transparent, rgba(255,255,255,0.1), transparent);
		transition: left 0.5s;
	}

	.skill-tag:hover::before {
		left: 100%;
	}

	.skill-tag:hover {
		background: var(--color-accent);
		color: white;
		transform: translateY(-4px);
		box-shadow: var(--shadow-soft);
	}

	.learning-tag {
		border-color: #fbbf24;
		color: #fbbf24;
		background: linear-gradient(135deg, rgba(251, 191, 36, 0.1), rgba(251, 191, 36, 0.05));
	}

	.learning-tag:hover {
		background-color: #fbbf24;
		color: var(--color-primary);
	}

	@media (max-width: 768px) {
		.skills {
			padding: 3rem 1rem;
		}

		.skills-grid {
			grid-template-columns: 1fr;
		}

		.category-icon {
			font-size: 2rem;
		}
	}
</style>
